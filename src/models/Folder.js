export class Folder {
    constructor(folderId, folderName, size, xFiles) {
      this.folderId = folderId;
      this.folderName = folderName;
      this.size = size;
      this.xFiles = xFiles;
    }
  }