export class File {
    constructor(id, name, type, content, size, folderId, folder) {
      this.id = id;
      this.name = name;
      this.type = type;
      this.content = content;
      this.size = size;
      this.folderId = folderId;
      this.folder = folder;
    }
  }
  